<Window x:Class="Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentation"
        xmlns:views="clr-namespace:Presentation.Views.Clientes"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Proyecto" Height="600" Width="1200">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="48"/>
                <RowDefinition Height="48"/>
                <RowDefinition Height="48"/>
                <RowDefinition />
            </Grid.RowDefinitions>

            <Menu Grid.Row="0">
                <MenuItem Header="Clientes" Click="HandleClientes"/>
            </Menu>

            <Menu Grid.Row="1">
                <MenuItem Header="Habitaciones" Click="HandleHabitaciones"/>
            </Menu>

            <Menu Grid.Row="2">
                <MenuItem Header="Reservaciones" Click="HandleReservaciones"/>
            </Menu>

            <Menu Grid.Row="3">
                <MenuItem Header="Usuarios" Click="HandleUsuarios"/>
            </Menu>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Menu Grid.Row="0" Height="48" Width="Auto" Padding="8" >
                <MenuItem Header="Consultar" Padding="8 0" Margin="4 0" Height="32" Click="HandleHotelReadView"/>
                <MenuItem Header="Agregar" Padding="8 0" Margin="4 0" Height="32" Click="HandleHotelInsertView"/>
                <MenuItem Header="Actualizar" Padding="8 0" Margin="4 0" Height="32" Click="HandleHotelUpdateView"/>
                <MenuItem Header="Borrar" Padding="8 0" Margin="4 0" Height="32" Click="HandleHotelDeleteView"/>
            </Menu>

            <ScrollViewer Grid.Row="1" Background="White">
                <ContentControl Content="{Binding}" />
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
