<Window x:Class="Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentation"
        xmlns:views="clr-namespace:Presentation.Views"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        Title="MainWindow" Height="600" Width="900">
    <Window.Resources>
        <DataTemplate x:Name="ProductView" DataType="{x:Type views:ProductView}">
            <views:ProductView DataContext="{Binding}" />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Menu Height="48" Width="Auto" Padding="8" >
            <MenuItem Header="Inventory" Padding="8 0" Margin="4 0" Height="32" Click="HandleInventoryView" />
            <MenuItem Header="Products" Padding="8 0" Margin="4 0" Height="32" Click="HandleProductsView"/>
            <MenuItem Header="Manufacturers" Padding="8 0" Margin="4 0" Height="32" Click="HandleManufacturersView" />
            <MenuItem Header="Inspections" Padding="8 0" Margin="4 0" Height="32" Click="HandleInspectionsView"/>
        </Menu>

        <ScrollViewer Grid.Row="1" Background="White">
            <ContentControl Content="{Binding}" />
        </ScrollViewer>
    </Grid>
</Window>
